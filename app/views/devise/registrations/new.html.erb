<div class='login_signup'>
  <h2>Sign Up</h2>

  <%= form_for(resource, :as => resource_name, :url => registration_path(resource_name), html: {class: "pure-form pure-form-aligned"}) do |f| %>
  <%= devise_error_messages! %>
  <fieldset>
    <div class="pure-control-group">


      <div><%= f.label :email %><span class="add-on"><i class="icon-envelope"></i></span>
        <%= f.email_field :email, :autofocus => true %></div>


        <div><%= f.label :password %><span class="add-on"><i class="icon-key"></i>  </span><%= f.password_field :password %></div>

          <div><%= f.label :password_confirmation %><span class="add-on"><i class="icon-key"></i>  </span><%= f.password_field :password_confirmation %></div>
            <div class="pure-controls">
              <%= f.submit "Sign up", class: "pure-button pure-button-primary" %>
              <%= render "devise/shared/links" %></div>
            </div>
          </fieldset>
          <% end %>
        </div>
